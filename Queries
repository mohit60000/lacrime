LA City Crime Queries: 
1) select * from (select * from crime order by date_occured desc) where rownum<=100;
2) "select crime_codes from crime where EXTRACT(year from date_reported)="+yearSelected;
3) select count(dr_number) as case_count, y as year \
    from (select dr_number, EXTRACT(year from date_reported) as y from crime) \
    group by y \
    order by y desc;
4)select * from (select latitude, longitude from area_details) where rownum<=10;
